# Read data from txt files. Using "read.table" is important, don't use "read.delim"
x <- read.table("your directory")
y <- read.table("your directory")

# Merge 2 data set by Gene names
z <- merge(x, y,  by.x = "Gene", by.y = "Gene")

#Write the newly created data into a new file
write.table(z, "//hlm/data/lab/Lab_Tsai/Tran/4 Tran/1 UV paper/UV Data/Core data/all.txt", sep="\t")


